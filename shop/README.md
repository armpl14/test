# Simple Online Shop

This is a minimal example of an online shop implemented with Node.js. No external dependencies are required.

## Running the shop

```bash
node index.js
```

The server will start on [http://localhost:3000](http://localhost:3000). Open this address in your browser to interact with the shop.
